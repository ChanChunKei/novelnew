# 如何找到"生成详情"按钮

## 步骤1：重启后端（让新代码生效）
```bash
# 方式A：systemd
sudo systemctl restart arboris-api

# 方式B：直接运行
pkill -f uvicorn
cd /Users/siyu/novelnew/backend
python3 -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload &
```

## 步骤2：刷新前端
在浏览器中按 Ctrl+Shift+R（或 Cmd+Shift+R）强制刷新

## 步骤3：生成一个新章节（使用3Agent模式）
1. 进入写作台
2. 选择一个章节
3. 点击"生成"或"重新生成"
4. 确保使用的是"3Agent对话模式"

## 步骤4：查看生成详情
章节生成完成后，在章节内容区域的右上角会出现：
```
┌─────────────────────────────────────┐
│ ✓ 这个章节已经完成                    │
│                                     │
│              [生成详情] [查看所有版本] │
└─────────────────────────────────────┘
```

点击"生成详情"即可查看metadata

## ⚠️ 如果看不到"生成详情"按钮

说明该章节没有metadata，可能因为：
- 章节是用旧代码生成的
- 章节不是用3Agent模式生成的（基础模式可能没有完整metadata）
- 后端数据有问题

可以重新生成一个章节测试。
